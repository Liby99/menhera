let
    max = (x, y) => if x > y then x else y,
    sum = (x, y) => x + y,
    get = (arr, i) => arr[i],
    fib = (i) => fib(i - 1) + fib(i - 2)
in
    max(sum(input[0], input[1]), input[2]) + ((i) => i + 2)(3)
