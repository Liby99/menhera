import {
    { println } from console,
    linked_list as ll
}

main (args) {
    let
        ll1 = ll::new(),
        ll2 = ll::push(ll::push(ll::push(ll1, 1), 2), 3),
        ll3 = ll::push(ll::push(ll1, 4), 5),
        ll4 = ll3 + ll2,
        eq = ll1 == ll1,
        eq2 = ll1 == ll2
    in
    match my_ll {
        ll::Node(e, l) => asdasdf,
        ll::Tail()
    }
}
